-r requirements-gigaam.lock.txt

# Locked local pyannote VAD path. Production fails closed without it; development
# uses emission-stitched fixed windows and labels the result as degraded.
numba==0.66.0
pyannote.audio==4.0.7
pyarrow==23.0.1
torchcodec==0.10.0
transformers==5.14.1
